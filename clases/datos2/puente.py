def sendlowers():

def solve(t:list,groups=2);
	t.sort()
	origin=t.copy()
	arrive=[]
	traveling=[]
	time=0
	minusArrive=False
	while origin==[]:
		timecost=max(traveling)+min(traveling)
		if not minusArrive and timecost<:
			traveling=[:groups]
			#groups
			minusArrive=True
			arrive.append(traveling.pop())
		else:
			traveling=[-1*groups:]
			arrive.remove(min(arrive))
			
			minusArrive=False
		t2.append(traveling.pop())
		

	time+=min(travelingp)
	for i in range(groups-1):# can pop 1 because is sorted
		arrive.append(t.pop())
	tmp=t.pop()
	time+=tmp


	#primero viajan menores
	#se devuelve el menor
	#viajan los 2 mayores
	#se devuelve el menor que queda
	#manda al menor
	#manda al mas rapido
	#mandar a uno de los mas rapidos

t=[10,7,5,3,2,1,1000]